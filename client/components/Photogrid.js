import React from 'react';
import { Link } from 'react-router';


const PhotGrid = React.createClass({
    render() {
        return(
            <div className="photo-grid">
                I'm the photogrid
            </div>
        )
    }
})

export default PhotGrid;